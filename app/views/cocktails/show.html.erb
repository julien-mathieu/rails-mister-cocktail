<div class="container mt-4">
	<h1> <%= @cocktail.name %> </h1>

	<% @cocktail.doses.each do |dose| %>
	  <p><%= dose.ingredient.name %>: <%= dose.description %></p>
	<% end %>

	<%= link_to "Ajouter un ingrédient à ce cocktail", new_cocktail_dose_path(@cocktail) %>
</div>